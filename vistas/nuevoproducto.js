import { StatusBar } from "expo-status-bar";
import React from "react";
import { ScrollView, View } from "react-native";


const NuevoProducto = ({navigator}) => {

    return (
        <View>
            <StatusBar></StatusBar>
            <ScrollView>
                
            </ScrollView>
        </View>
    );
}